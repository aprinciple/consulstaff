document.addEventListener('DOMContentLoaded', () => {
  const pagePostProject = document.querySelector('.page-post-project');
  
  if (pagePostProject) {
    const htmlEl = document.documentElement;
    htmlEl.style.scrollBehavior = 'smooth';
  }
});
